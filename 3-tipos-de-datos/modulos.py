#Modulos!!!
#     --> Es un archivo que contiene codigo que quiero incluir en mi programa. Se usa el "import" para incluir un modulo (contruido por python). Sirve para dividir un programa largo y reusar sus partes en distintos archivos.

import math as m #Le doy un "alias" para usarlo en el codigo. Para llamar a sus atributos uso alias.atributo
from math import pi #Ya no es necesario usar m.pi
num_pi = pi
print(num_pi)

